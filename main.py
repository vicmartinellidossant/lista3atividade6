priceUni = float(input("Qual é o preço de cada unidade do produto? "))

quantVendas = int(input("Qual é a quantidade vendida? "))

percentDescont = float(input("Qual é o percentual de desconto para a venda? "))

sale = percentDescont / 100

